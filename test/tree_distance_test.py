import unittest

class TestDistanceMethods(unittest.TestCase):

    """
    TEST CASES:
    - (1) Test that two identical trees have BSD distance of 0
    - (2) Test that two example trees from paper have specified distance:
    check eBSD, dBSD, BSD, etc.
    - (3) Test that if tree is invalid, exception is thrown properly
    """

    #def test_tree_1(tree_1, tree_1):
        #self.assertEqual(0, get_bsd(tree_1, tree_1))

if __name__ == "__main__":
    unittest.main()
